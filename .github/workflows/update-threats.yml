name: Update Threat Feeds

on:
  schedule:
    - cron: '0 2 * * *' # daily at 02:00 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run fetch script
        run: |
          python scripts/fetch_threats.py

      - name: Show generated threats data (debug)
        run: |
          echo "_data directory contents:"; ls -la _data || true
          echo "---- threats.yml (first 200 lines) ----"; sed -n '1,200p' _data/threats.yml || true
          echo "---- end threats.yml ----"

      - name: Commit changes
        run: |
          echo "git status before commit:"; git status --porcelain || true
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -n "$(git status --porcelain)" ]; then
            git add _data/threats.yml
            git commit -m "chore: update threat feeds"
            git push
          else
            echo "No changes to commit"
          fi